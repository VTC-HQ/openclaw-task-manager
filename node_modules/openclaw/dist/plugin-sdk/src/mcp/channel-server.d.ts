import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import { type OpenClawConfig } from "../config/config.js";
import { OpenClawChannelBridge } from "./channel-bridge.js";
import { type ClaudeChannelMode } from "./channel-shared.js";
export { OpenClawChannelBridge } from "./channel-bridge.js";
export type OpenClawMcpServeOptions = {
    gatewayUrl?: string;
    gatewayToken?: string;
    gatewayPassword?: string;
    config?: OpenClawConfig;
    claudeChannelMode?: ClaudeChannelMode;
    verbose?: boolean;
};
export declare function createOpenClawChannelMcpServer(opts?: OpenClawMcpServeOptions): Promise<{
    server: McpServer;
    bridge: OpenClawChannelBridge;
    start: () => Promise<void>;
    close: () => Promise<void>;
}>;
export declare function serveOpenClawChannelMcp(opts?: OpenClawMcpServeOptions): Promise<void>;
