type IsMainModuleOptions = {
    currentFile: string;
    argv?: string[];
    env?: NodeJS.ProcessEnv;
    cwd?: string;
    wrapperEntryPairs?: Array<{
        wrapperBasename: string;
        entryBasename: string;
    }>;
};
export declare function isMainModule({ currentFile, argv, env, cwd, wrapperEntryPairs, }: IsMainModuleOptions): boolean;
export {};
