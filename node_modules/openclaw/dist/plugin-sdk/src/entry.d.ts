#!/usr/bin/env node
export declare function tryHandleRootHelpFastPath(argv: string[], deps?: {
    outputRootHelp?: () => void | Promise<void>;
    onError?: (error: unknown) => void;
    env?: NodeJS.ProcessEnv;
}): boolean;
