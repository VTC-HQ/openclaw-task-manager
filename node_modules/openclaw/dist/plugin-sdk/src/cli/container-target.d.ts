import { spawnSync } from "node:child_process";
type CliContainerParseResult = {
    ok: true;
    container: string | null;
    argv: string[];
} | {
    ok: false;
    error: string;
};
export type CliContainerTargetResult = {
    handled: true;
    exitCode: number;
} | {
    handled: false;
    argv: string[];
};
type ContainerTargetDeps = {
    env: NodeJS.ProcessEnv;
    spawnSync: typeof spawnSync;
    stdinIsTTY: boolean;
    stdoutIsTTY: boolean;
};
export declare function parseCliContainerArgs(argv: string[]): CliContainerParseResult;
export declare function resolveCliContainerTarget(argv: string[], env?: NodeJS.ProcessEnv): string | null;
export declare function maybeRunCliInContainer(argv: string[], deps?: Partial<ContainerTargetDeps>): CliContainerTargetResult;
export {};
