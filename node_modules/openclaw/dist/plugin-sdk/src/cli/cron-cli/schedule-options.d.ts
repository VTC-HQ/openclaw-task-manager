import type { CronSchedule } from "../../cron/types.js";
type ScheduleOptionInput = {
    at?: unknown;
    cron?: unknown;
    every?: unknown;
    exact?: unknown;
    stagger?: unknown;
    tz?: unknown;
};
export type CronEditScheduleRequest = {
    kind: "direct";
    schedule: CronSchedule;
} | {
    kind: "patch-existing-cron";
    staggerMs: number | undefined;
    tz: string | undefined;
} | {
    kind: "none";
};
export declare function resolveCronCreateSchedule(options: ScheduleOptionInput): CronSchedule;
export declare function resolveCronEditScheduleRequest(options: ScheduleOptionInput): CronEditScheduleRequest;
export declare function applyExistingCronSchedulePatch(existingSchedule: CronSchedule, request: Extract<CronEditScheduleRequest, {
    kind: "patch-existing-cron";
}>): CronSchedule;
export {};
