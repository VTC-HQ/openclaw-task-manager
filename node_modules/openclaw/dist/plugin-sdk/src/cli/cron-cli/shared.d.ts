import type { CronJob } from "../../cron/types.js";
import { type RuntimeEnv } from "../../runtime.js";
import type { GatewayRpcOpts } from "../gateway-rpc.js";
export declare const getCronChannelOptions: () => string;
export declare function printCronJson(value: unknown): void;
export declare function handleCronCliError(err: unknown): void;
export declare function warnIfCronSchedulerDisabled(opts: GatewayRpcOpts): Promise<void>;
export declare function parseDurationMs(input: string): number | null;
export declare function parseCronStaggerMs(params: {
    staggerRaw: string;
    useExact: boolean;
}): number | undefined;
/**
 * Parse a one-shot `--at` value into an ISO string (UTC).
 *
 * When `tz` is provided and the input is an offset-less datetime
 * (e.g. `2026-03-23T23:00:00`), the datetime is interpreted in
 * that IANA timezone instead of UTC.
 */
export declare function parseAt(input: string, tz?: string): string | null;
export declare function printCronList(jobs: CronJob[], runtime?: RuntimeEnv): void;
