import type { PendingRequest } from "./types.js";
export declare function renderPendingPairingRequestsTable(params: {
    pending: PendingRequest[];
    now: number;
    tableWidth: number;
    theme: {
        heading: (text: string) => string;
        warn: (text: string) => string;
        muted: (text: string) => string;
    };
}): {
    heading: string;
    table: string;
};
