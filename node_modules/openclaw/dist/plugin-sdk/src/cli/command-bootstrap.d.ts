import type { RuntimeEnv } from "../runtime.js";
export declare function ensureCliCommandBootstrap(params: {
    runtime: RuntimeEnv;
    commandPath: string[];
    suppressDoctorStdout?: boolean;
    skipConfigGuard?: boolean;
    allowInvalid?: boolean;
    loadPlugins?: boolean;
}): Promise<void>;
