import type { CliDeps } from "../cli/deps.js";
import type { OpenClawConfig } from "../config/types.js";
import { resolveFailureDestination, type CronFailureDeliveryPlan, type CronFailureDestinationInput, type CronDeliveryPlan, resolveCronDeliveryPlan } from "./delivery-plan.js";
export { resolveCronDeliveryPlan, resolveFailureDestination, type CronDeliveryPlan, type CronFailureDeliveryPlan, type CronFailureDestinationInput, };
export declare function sendFailureNotificationAnnounce(deps: CliDeps, cfg: OpenClawConfig, agentId: string, jobId: string, target: {
    channel?: string;
    to?: string;
    accountId?: string;
    sessionKey?: string;
}, message: string): Promise<void>;
