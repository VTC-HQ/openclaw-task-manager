import { runEmbeddedPiAgent } from "../agents/pi-embedded.js";
import type { CliDeps } from "../cli/deps.js";
import { runCronIsolatedAgentTurn } from "./isolated-agent.js";
export declare function createCliDeps(overrides?: Partial<CliDeps>): CliDeps;
export declare function mockAgentPayloads(payloads: Array<Record<string, unknown>>, extra?: Partial<Awaited<ReturnType<typeof runEmbeddedPiAgent>>>): void;
export declare function expectDirectTelegramDelivery(deps: CliDeps, params: {
    chatId: string;
    text: string;
    messageThreadId?: number;
}): void;
export declare function runTelegramAnnounceTurn(params: {
    home: string;
    storePath: string;
    deps: CliDeps;
    delivery: {
        mode: "announce";
        channel: string;
        to?: string;
        bestEffort?: boolean;
    };
    deliveryContract?: "cron-owned" | "shared";
}): Promise<Awaited<ReturnType<typeof runCronIsolatedAgentTurn>>>;
