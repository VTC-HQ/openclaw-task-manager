export declare function withTempDir<T>(options: {
    prefix: string;
    parentDir?: string;
    subdir?: string;
}, run: (dir: string) => Promise<T>): Promise<T>;
export declare function createSuiteTempRootTracker(options: {
    prefix: string;
    parentDir?: string;
}): {
    setup(): Promise<string>;
    make(prefix?: string): Promise<string>;
    cleanup(): Promise<void>;
};
export declare function withTempDirSync<T>(options: {
    prefix: string;
    parentDir?: string;
    subdir?: string;
}, run: (dir: string) => T): T;
