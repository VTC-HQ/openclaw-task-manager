export declare function seedSessionStore(params: {
    storePath: string;
    sessionKey: string;
    compactionCount: number;
    updatedAt?: number;
}): Promise<void>;
export declare function readCompactionCount(storePath: string, sessionKey: string): Promise<number>;
export declare function waitForCompactionCount(params: {
    storePath: string;
    sessionKey: string;
    expected: number;
}): Promise<void>;
