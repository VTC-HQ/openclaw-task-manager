import type { SandboxContext } from "../sandbox.js";
import type { SandboxFsBridge } from "../sandbox/fs-bridge.js";
export declare function createUnsafeMountedBridge(params: {
    root: string;
    agentHostRoot: string;
    workspaceContainerRoot?: string;
}): SandboxFsBridge;
export declare function createUnsafeMountedSandbox(params: {
    sandboxRoot: string;
    agentRoot: string;
    workspaceContainerRoot?: string;
}): SandboxContext;
export declare function withUnsafeMountedSandboxHarness(run: (ctx: {
    sandboxRoot: string;
    agentRoot: string;
    sandbox: SandboxContext;
}) => Promise<void>): Promise<void>;
