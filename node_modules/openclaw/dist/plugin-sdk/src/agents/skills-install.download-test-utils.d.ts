export declare function setTempStateDir(workspaceDir: string): string;
export declare function withTempWorkspace(run: (params: {
    workspaceDir: string;
    stateDir: string;
}) => Promise<void>): Promise<void>;
export declare function writeDownloadSkill(params: {
    workspaceDir: string;
    name: string;
    installId: string;
    url: string;
    archive: "tar.gz" | "tar.bz2" | "zip";
    stripComponents?: number;
    targetDir: string;
}): Promise<string>;
