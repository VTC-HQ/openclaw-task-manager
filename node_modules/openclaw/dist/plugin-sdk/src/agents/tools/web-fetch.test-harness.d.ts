import type { LookupFn } from "../../infra/net/ssrf.js";
export declare function makeFetchHeaders(map: Record<string, string>): {
    get: (key: string) => string | null;
};
export declare function createBaseWebFetchToolConfig(opts?: {
    maxResponseBytes?: number;
    lookupFn?: LookupFn;
}): {
    config: {
        tools: {
            web: {
                fetch: {
                    cacheTtlMinutes: number;
                    firecrawl: {
                        enabled: boolean;
                    };
                    maxResponseBytes?: number;
                };
            };
        };
    };
    lookupFn?: LookupFn;
};
