import { type Mock } from "vitest";
export type ReplyRuntimeMocks = {
    runEmbeddedPiAgent: Mock;
    loadModelCatalog: Mock;
    webAuthExists: Mock;
    getWebAuthAgeMs: Mock;
    readWebSelfId: Mock;
};
export declare function createTempHomeHarness(options: {
    prefix: string;
    beforeEachCase?: () => void;
}): {
    withTempHome: <T>(fn: (home: string) => Promise<T>) => Promise<T>;
};
export declare function makeReplyConfig(home: string): {
    agents: {
        defaults: {
            model: string;
            workspace: string;
        };
    };
    channels: {
        whatsapp: {
            allowFrom: string[];
        };
    };
    session: {
        store: string;
    };
};
export declare function createReplyRuntimeMocks(): ReplyRuntimeMocks;
export declare function installReplyRuntimeMocks(mocks: ReplyRuntimeMocks): void;
export declare function resetReplyRuntimeMocks(mocks: ReplyRuntimeMocks): void;
export declare function makeEmbeddedTextResult(text: string): {
    payloads: {
        text: string;
    }[];
    meta: {
        durationMs: number;
        agentMeta: {
            sessionId: string;
            provider: string;
            model: string;
        };
    };
};
