export type RuntimeSourceGuardrailFile = {
    relativePath: string;
    source: string;
};
export declare function shouldSkipGuardrailRuntimeSource(relativePath: string): boolean;
export declare function loadRuntimeSourceFilesForGuardrails(repoRoot: string): Promise<RuntimeSourceGuardrailFile[]>;
