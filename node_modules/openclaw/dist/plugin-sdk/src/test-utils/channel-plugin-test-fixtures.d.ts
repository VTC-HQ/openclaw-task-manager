import type { ChannelPlugin } from "../channels/plugins/types.js";
export declare function makeDirectPlugin(params: {
    id: string;
    label: string;
    docsPath: string;
    config: ChannelPlugin["config"];
}): ChannelPlugin;
