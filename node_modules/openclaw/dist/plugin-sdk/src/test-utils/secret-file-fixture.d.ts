export type SecretFiles = {
    passwordFile?: string;
    tokenFile?: string;
};
export declare function withTempSecretFiles<T>(prefix: string, secrets: {
    password?: string;
    token?: string;
}, run: (files: SecretFiles) => Promise<T>): Promise<T>;
