export declare function createBundleMcpTempHarness(): {
    createTempDir(prefix: string): Promise<string>;
    cleanup(): Promise<void>;
};
export declare function resolveBundlePluginRoot(homeDir: string, pluginId: string): string;
export declare function writeClaudeBundleManifest(params: {
    homeDir: string;
    pluginId: string;
    manifest: Record<string, unknown>;
}): Promise<string>;
export declare function writeBundleTextFiles(rootDir: string, files: Readonly<Record<string, string>>): Promise<void>;
export declare function createEnabledPluginEntries(pluginIds: readonly string[]): {
    [k: string]: {
        enabled: boolean;
    };
};
export declare function createBundleProbePlugin(homeDir: string): Promise<{
    pluginRoot: string;
    serverPath: string;
}>;
export declare function withBundleHomeEnv<T>(tempHarness: {
    createTempDir: (prefix: string) => Promise<string>;
}, prefix: string, run: (params: {
    homeDir: string;
    workspaceDir: string;
}) => Promise<T>): Promise<T>;
