import { type RuntimeEnv } from "../runtime.js";
export type SessionsCleanupOptions = {
    store?: string;
    agent?: string;
    allAgents?: boolean;
    dryRun?: boolean;
    enforce?: boolean;
    activeKey?: string;
    json?: boolean;
    fixMissing?: boolean;
};
export declare function sessionsCleanupCommand(opts: SessionsCleanupOptions, runtime: RuntimeEnv): Promise<void>;
