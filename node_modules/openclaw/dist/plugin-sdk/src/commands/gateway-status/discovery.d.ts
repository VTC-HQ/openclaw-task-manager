import type { GatewayBonjourBeacon } from "../../infra/bonjour-discovery.js";
import { serializeGatewayDiscoveryBeacon } from "../../infra/gateway-discovery-targets.js";
export declare function inferSshTargetFromRemoteUrl(rawUrl?: string | null): string | null;
export declare function buildSshTarget(input: {
    user?: string;
    host?: string;
    port?: number;
}): string | null;
export declare function resolveSshTarget(params: {
    rawTarget: string;
    identity: string | null;
    overallTimeoutMs: number;
    loadSshConfigModule: () => Promise<typeof import("../../infra/ssh-config.js")>;
    loadSshTunnelModule: () => Promise<typeof import("../../infra/ssh-tunnel.js")>;
}): Promise<{
    target: string;
    identity?: string;
} | null>;
export declare function pickAutoSshTargetFromDiscovery(params: {
    discovery: GatewayBonjourBeacon[];
    parseSshTarget: (target: string) => unknown;
    sshUser?: string | null;
}): string | null;
export { serializeGatewayDiscoveryBeacon };
