import { type RuntimeEnv } from "../runtime.js";
export declare function statusJsonCommand(opts: {
    deep?: boolean;
    usage?: boolean;
    timeoutMs?: number;
    all?: boolean;
}, runtime: RuntimeEnv): Promise<void>;
