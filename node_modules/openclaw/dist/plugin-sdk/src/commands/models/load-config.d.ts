import type { RuntimeEnv } from "../../runtime.js";
import { type OpenClawConfig } from "./load-config.runtime.js";
export type LoadedModelsConfig = {
    sourceConfig: OpenClawConfig;
    resolvedConfig: OpenClawConfig;
    diagnostics: string[];
};
export declare function loadModelsConfigWithSource(params: {
    commandName: string;
    runtime?: RuntimeEnv;
}): Promise<LoadedModelsConfig>;
export declare function loadModelsConfig(params: {
    commandName: string;
    runtime?: RuntimeEnv;
}): Promise<OpenClawConfig>;
