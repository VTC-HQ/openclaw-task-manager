import { type ErrorShape } from "../../protocol/index.js";
export type UnauthorizedFloodGuardOptions = {
    closeAfter?: number;
    logEvery?: number;
};
export type UnauthorizedFloodDecision = {
    shouldClose: boolean;
    shouldLog: boolean;
    count: number;
    suppressedSinceLastLog: number;
};
export declare class UnauthorizedFloodGuard {
    private readonly closeAfter;
    private readonly logEvery;
    private count;
    private suppressedSinceLastLog;
    constructor(options?: UnauthorizedFloodGuardOptions);
    registerUnauthorized(): UnauthorizedFloodDecision;
    reset(): void;
}
export declare function isUnauthorizedRoleError(error?: ErrorShape): boolean;
