import { WebSocket } from "ws";
import { type DeviceIdentity } from "../infra/device-identity.js";
export declare function resolveDeviceIdentityPath(name: string): string;
export declare function loadDeviceIdentity(name: string): {
    identityPath: string;
    identity: DeviceIdentity;
    publicKey: string;
};
export declare function pairDeviceIdentity(params: {
    name: string;
    role: "node" | "operator";
    scopes: string[];
    clientId?: string;
    clientMode?: string;
}): Promise<{
    identityPath: string;
    identity: DeviceIdentity;
    publicKey: string;
}>;
export declare function issueOperatorToken(params: {
    name: string;
    approvedScopes: string[];
    tokenScopes?: string[];
    clientId?: string;
    clientMode?: string;
}): Promise<{
    deviceId: string;
    identityPath: string;
    token: string;
}>;
export declare function openTrackedWs(port: number): Promise<WebSocket>;
