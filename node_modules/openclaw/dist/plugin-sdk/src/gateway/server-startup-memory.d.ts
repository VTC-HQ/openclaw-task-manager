import type { OpenClawConfig } from "../config/config.js";
export declare function startGatewayMemoryBackend(params: {
    cfg: OpenClawConfig;
    log: {
        info?: (msg: string) => void;
        warn: (msg: string) => void;
    };
}): Promise<void>;
