/**
 * OpenResponses HTTP Handler
 *
 * Implements the OpenResponses `/v1/responses` endpoint for OpenClaw Gateway.
 *
 * @see https://www.open-responses.com/
 */
import type { IncomingMessage, ServerResponse } from "node:http";
import type { GatewayHttpResponsesConfig } from "../config/types.gateway.js";
import type { AuthRateLimiter } from "./auth-rate-limit.js";
import type { ResolvedGatewayAuth } from "./auth.js";
type OpenResponsesHttpOptions = {
    auth: ResolvedGatewayAuth;
    maxBodyBytes?: number;
    config?: GatewayHttpResponsesConfig;
    trustedProxies?: string[];
    allowRealIpFallback?: boolean;
    rateLimiter?: AuthRateLimiter;
};
declare function wrapUntrustedFileContent(content: string): string;
type ResponseSessionScope = {
    authSubject: string;
    agentId: string;
    requestedSessionKey?: string;
};
export declare const __testing: {
    resetResponseSessionState(): void;
    wrapUntrustedFileContent: typeof wrapUntrustedFileContent;
    storeResponseSessionAt(responseId: string, sessionKey: string, now: number, scope?: ResponseSessionScope): void;
    lookupResponseSessionAt(responseId: string | undefined, now: number, scope?: ResponseSessionScope): string | undefined;
    getResponseSessionIds(): string[];
};
export { buildAgentPrompt } from "./openresponses-prompt.js";
export declare function handleOpenResponsesHttpRequest(req: IncomingMessage, res: ServerResponse, opts: OpenResponsesHttpOptions): Promise<boolean>;
