import type { loadConfig } from "../config/config.js";
import type { PluginRegistry } from "../plugins/registry.js";
import type { GatewayRequestHandler } from "./server-methods/types.js";
type GatewayPluginBootstrapLog = {
    info: (msg: string) => void;
    warn: (msg: string) => void;
    error: (msg: string) => void;
    debug: (msg: string) => void;
};
type GatewayPluginBootstrapParams = {
    cfg: ReturnType<typeof loadConfig>;
    activationSourceConfig?: ReturnType<typeof loadConfig>;
    workspaceDir: string;
    log: GatewayPluginBootstrapLog;
    coreGatewayHandlers: Record<string, GatewayRequestHandler>;
    baseMethods: string[];
    pluginIds?: string[];
    preferSetupRuntimeForChannelPlugins?: boolean;
    logDiagnostics?: boolean;
    beforePrimeRegistry?: (pluginRegistry: PluginRegistry) => void;
};
export declare function prepareGatewayPluginLoad(params: GatewayPluginBootstrapParams): {
    pluginRegistry: PluginRegistry;
    gatewayMethods: string[];
};
export declare function loadGatewayStartupPlugins(params: Omit<GatewayPluginBootstrapParams, "beforePrimeRegistry">): {
    pluginRegistry: PluginRegistry;
    gatewayMethods: string[];
};
export declare function reloadDeferredGatewayPlugins(params: Omit<GatewayPluginBootstrapParams, "beforePrimeRegistry" | "preferSetupRuntimeForChannelPlugins">): {
    pluginRegistry: PluginRegistry;
    gatewayMethods: string[];
};
export {};
