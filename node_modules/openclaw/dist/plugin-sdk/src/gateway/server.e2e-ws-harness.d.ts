import { WebSocket } from "ws";
import { connectOk, startGatewayServer } from "./test-helpers.js";
export type GatewayWsClient = {
    ws: WebSocket;
    hello: unknown;
};
export type GatewayServerHarness = {
    port: number;
    server: Awaited<ReturnType<typeof startGatewayServer>>;
    openClient: (opts?: Parameters<typeof connectOk>[1]) => Promise<GatewayWsClient>;
    close: () => Promise<void>;
};
export declare function startGatewayServerHarness(): Promise<GatewayServerHarness>;
