export declare function createTranscriptFixtureSync(params: {
    prefix: string;
    sessionId: string;
    fileName?: string;
}): {
    dir: string;
    transcriptPath: string;
};
export declare function createMockSessionEntry(params: {
    transcriptPath: string;
    sessionId: string;
    canonicalKey?: string;
    cfg?: Record<string, unknown>;
}): {
    cfg: Record<string, unknown>;
    storePath: string;
    entry: {
        sessionId: string;
        sessionFile: string;
    };
    canonicalKey: string;
};
