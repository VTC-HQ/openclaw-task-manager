import { type Mock } from "vitest";
import type { OpenClawConfig } from "../../config/types.openclaw.js";
import type { GatewayRequestHandlerOptions } from "./types.js";
type UnknownMock = Mock<(...args: unknown[]) => unknown>;
type GatewayLogMocks = {
    error: UnknownMock;
    warn: UnknownMock;
    info: UnknownMock;
    debug: UnknownMock;
};
type ConfigHandlerHarness = {
    options: GatewayRequestHandlerOptions;
    respond: UnknownMock;
    logGateway: GatewayLogMocks;
    disconnectClientsUsingSharedGatewayAuth: UnknownMock;
};
export declare function createConfigWriteSnapshot(config: OpenClawConfig): {
    snapshot: {
        path: string;
        exists: boolean;
        raw: string;
        parsed: OpenClawConfig;
        sourceConfig: OpenClawConfig;
        resolved: OpenClawConfig;
        valid: boolean;
        runtimeConfig: OpenClawConfig;
        config: OpenClawConfig;
        hash: string;
        issues: never[];
        warnings: never[];
        legacyIssues: never[];
    };
    writeOptions: Record<string, never>;
};
export declare function createConfigHandlerHarness(args?: {
    method?: string;
    params?: unknown;
    overrides?: Partial<GatewayRequestHandlerOptions>;
    contextOverrides?: Partial<GatewayRequestHandlerOptions["context"]>;
}): ConfigHandlerHarness;
export declare function flushConfigHandlerMicrotasks(): Promise<void>;
export {};
