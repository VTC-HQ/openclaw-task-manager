import type { ConfigUiHints } from "./schema.js";
import type { ConfigFileSnapshot } from "./types.openclaw.js";
export type TestSnapshot<TConfig extends Record<string, unknown>> = ConfigFileSnapshot & {
    parsed: TConfig;
    sourceConfig: TConfig;
    resolved: TConfig;
    runtimeConfig: TConfig;
    config: TConfig;
};
export declare function makeSnapshot<TConfig extends Record<string, unknown>>(config: TConfig, raw?: string): TestSnapshot<TConfig>;
export declare function restoreRedactedValues<TOriginal>(incoming: unknown, original: TOriginal, hints?: ConfigUiHints): TOriginal;
