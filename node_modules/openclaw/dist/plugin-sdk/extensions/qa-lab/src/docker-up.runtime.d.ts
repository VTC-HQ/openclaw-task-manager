type QaDockerUpResult = {
    outputDir: string;
    composeFile: string;
    qaLabUrl: string;
    gatewayUrl: string;
    stopCommand: string;
};
type RunCommand = (command: string, args: string[], cwd: string) => Promise<{
    stdout: string;
    stderr: string;
}>;
type FetchLike = (input: string) => Promise<{
    ok: boolean;
}>;
declare function resolveHostPort(preferredPort: number, pinned: boolean): Promise<number>;
export declare function runQaDockerUp(params: {
    repoRoot?: string;
    outputDir?: string;
    gatewayPort?: number;
    qaLabPort?: number;
    providerBaseUrl?: string;
    image?: string;
    usePrebuiltImage?: boolean;
    bindUiDist?: boolean;
    skipUiBuild?: boolean;
}, deps?: {
    runCommand?: RunCommand;
    fetchImpl?: FetchLike;
    sleepImpl?: (ms: number) => Promise<unknown>;
    resolveHostPortImpl?: typeof resolveHostPort;
}): Promise<QaDockerUpResult>;
export {};
