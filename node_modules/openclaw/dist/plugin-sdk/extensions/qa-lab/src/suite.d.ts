import { startQaLabServer } from "./lab-server.js";
import { type QaProviderMode } from "./model-selection.js";
import { type QaReportCheck } from "./report.js";
type QaSuiteScenarioResult = {
    name: string;
    status: "pass" | "fail";
    steps: QaReportCheck[];
    details?: string;
};
export type QaSuiteResult = {
    outputDir: string;
    reportPath: string;
    summaryPath: string;
    report: string;
    scenarios: QaSuiteScenarioResult[];
    watchUrl: string;
};
export declare function runQaSuite(params?: {
    repoRoot?: string;
    outputDir?: string;
    providerMode?: QaProviderMode | "live-openai";
    primaryModel?: string;
    alternateModel?: string;
    fastMode?: boolean;
    scenarioIds?: string[];
    lab?: Awaited<ReturnType<typeof startQaLabServer>>;
}): Promise<{
    outputDir: string;
    reportPath: string;
    summaryPath: string;
    report: string;
    scenarios: QaSuiteScenarioResult[];
    watchUrl: string;
}>;
export {};
